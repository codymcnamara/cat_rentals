<!DOCTYPE html>
<html>
<head>
  <title>Cats99</title>
  <%= csrf_meta_tags %>
</head>
<body>
<% if current_user.nil? %>
  <p> <%= link_to "Login!", new_session_url%> |
      <%= link_to "Create New User!", new_user_url%> </p>

<% else %>
  <h1>Welcome to the best site ever <%= current_user.username %></h1>
  <p> <%= button_to "Logout :(", session_url, method: :delete %></p>
<% end %>

<%= yield %>

</body>
</html>
